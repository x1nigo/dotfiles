#! /bin/sh

# This script should be able to find every single .pdf or .epub file under your
# home directory.

file="$(find $HOME/ | grep -Ei '\.pdf$|\.epub$' | dmenu)"
[ -z "$file" ] && exit 1

st -e zathura "$file"
