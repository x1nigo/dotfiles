#! /bin/sh

# The script looks for pdf files here:
texdir="${XDG_DOCUMENTS_DIR:-$HOME/dox}/"

texfile=$(find $texdir | grep ".\+\.pdf$" | dmenu -p "Open Pdf File:" -l 12)

[ -z $texfile ] && notify-send -t 6000 "⛔ No file selected." || st -e zathura "$texfile"
