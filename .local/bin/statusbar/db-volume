#!/bin/sh

case $BUTTON in
        1) wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle ;;
        3) notify-send "🔈 Volume module:" "\[Click to toggle mute\]
📣 loudest
🔊 high
🔉 medium
🔈 low
🔇 muted speaker" ;;
	4) setsid -f ${TERMINAL:-st} -e pulsemixer ;;
        5) st -e $EDITOR ${XDG_CONFIG_HOME:-$HOME/.config}/dwmblocks/blocks.h ;;
esac

vol=$(wpctl get-volume @DEFAULT_AUDIO_SINK@)

[ "$vol" != "${vol%\[MUTED\]}" ] && echo 🔇&& exit

vol="${vol#Volume: }"

split() {
	# For ommiting the . without calling an external program.
	IFS=$2
	set -- $1
	printf '%s' "$@"
}

vol="$(printf "%.0f" "$(split "$vol" ".")")"

case 1 in
	$((vol >= 100)) ) icon="📣" ;;
	$((vol >= 70)) ) icon="🔊" ;;
	$((vol >= 30)) ) icon="🔉" ;;
	$((vol >= 1)) ) icon="🔈" ;;
	* ) echo 🔇 && exit ;;
esac

echo "$icon$vol%"
