#! /bin/sh

# Install the systat package first.

cpu=$(mpstat | awk '/all/ { print int(100-$13) }') && icon="📀"

case $BUTTON in
	1) st -e gotop ;;
	3) st -e $EDITOR ~/.local/bin/statusbar/db-cpu ;;
	5) st -e $EDITOR ~/.config/dwmblocks/blocks.h ;;
esac

printf "%s%s%%\n" "$icon" "$cpu"
