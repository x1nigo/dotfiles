#!/bin/sh

mem=$(free | awk '/Mem:/ { print int($3 * 100 / $2) }') && icon="💾"

case $BUTTON in
	1) $TERMINAL -e htop ;;
	3) notify-send "💾 Memory module" "Click to run \`htop\` program and see system processes." ;;
	5) $TERMINAL -e $EDITOR "$0" ;;
esac

printf "%s%s%%\n" "$icon" "$mem"
