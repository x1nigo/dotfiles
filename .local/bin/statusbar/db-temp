#!/bin/sh

tmp=$(awk '{ print $0 / 1000 }' /sys/devices/platform/coretemp.0/hwmon/hwmon*/temp1_input)

if [ $tmp -gt 80 ]; then
	icon="💥"
elif [ $tmp -gt 60 ]; then
	icon="🔥"
elif [ $tmp -ge 0 ]; then
	icon="🥶"
fi

printf "%s%s°C" "$icon" "$tmp"
