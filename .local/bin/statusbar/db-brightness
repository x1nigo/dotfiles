#!/bin/sh

TOT="$(cat /sys/class/backlight/*/max_brightness)"
CUR="$(cat /sys/class/backlight/*/brightness)"
BRI=$(( $CUR * 100 / $TOT ))

[ "$BRI" -gt "50" ] && icon=🌅 || icon=☔

printf "%s%s%%" "$icon" "$BRI"
