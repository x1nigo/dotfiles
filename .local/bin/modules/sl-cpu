#!/bin/sh

temp="$(sensors | awk '/Core 0:/ {print $3}' | sed 's/+//;s/\..*//')"
case 1 in
	$((temp >= 60)) ) icon="🔥" ;;
	$((temp >= 45)) ) icon="🥵" ;;
	$((temp >= 1)) ) icon="🥶" ;;
esac

printf "%s%s°C\\n" "$icon" "$temp"
