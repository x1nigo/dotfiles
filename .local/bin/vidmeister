#!/bin/sh

# If you need to add more video file types, you can edit the line to
# include such items. Just separate them with the "|" delimiter.

file="$(find $HOME/ | grep -Ei '\.mp4$|\.mkv$' | dmenu -l 16)"
[ -z "$file" ] && exit 1

st -e mpv "$file"
