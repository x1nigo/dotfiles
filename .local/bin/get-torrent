#! /bin/sh

url=$(echo -n '' | dmenu -p "Enter torrent file url to download:")
[ -z $url ] && exit 1

transmission-remote -a $url && notify-send "✅ Torrent added!" "'$url'" ||
        notify-send "💀 Unable to add torrent." "Please try again."
