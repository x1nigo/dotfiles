#!/bin/sh

# This script types out the selected bookmark in conjunction with the `dm-bookmark` script.
# `xdotool` needs to be installed.

url="$(cat $HOME/.local/share/01-bookmarks | dmenu -i -l 35 -p "INSERT (ESC to cancel):" | cut -d' ' -f1)"
[ -z "$url" ] && exit 1

xdotool type "$url"
