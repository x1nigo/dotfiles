#!/bin/sh

act="$(printf "%s\n" "🛌 shut this computer down" "🔃 restart this machine" | dmenu -fn Monospace-12 -l 5 -p "System configuration action:")"
if ls -l /sbin/init | grep -q runit; then
	sys=loginctl
else
	sys=systemctl
fi

case $act in
	🛌*) $sys poweroff ;;
	🔃*) $sys reboot ;;
esac
