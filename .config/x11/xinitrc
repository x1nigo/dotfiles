#!/bin/sh

# Increases autorepeat rate.
xset r rate 300 50
# xrandr --dpi 96 # Adjust this depending on screen resolution.
xsetroot -cursor_name left_ptr
xrdb ${XDG_CONFIG_HOME:-$HOME/.config}/x11/xresources

# Autostart function that avoids duplication.
for program in mpd dunst unclutter picom pipewire dm-wallpaper dwmblocks transmission-daemon ; do
	pgrep -x "$program" || "$program" &
done >/dev/null 2>&1

# Start the dynamic window manager.
while true; do ssh-agent dwm; done
