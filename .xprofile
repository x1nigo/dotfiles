#!/bin/sh

xrandr --dpi 96 # You can set this higher for larger monitors.
sw & # Render the background

autostart="dunst picom pipewire wireplumber dwmblocks"

for program in $autostart; do
        pidof -s "$program" || "$program" &
done >/dev/null 2>&1
